<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.id, task %> : <%= task.title %> &gt; <%= task.content %> [<%= task.status %>]</li>
  <% end %>
</ul>
<%= paginate @tasks %>

<%= link_to 'タスクを新規作成', new_task_path %>